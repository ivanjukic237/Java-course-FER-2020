package hr.fer.zemris.java.hw06.shell;

/**
 * Enum of the shell which contains to statuses. Continue if the shell should
 * continue running and terminate if the shell should exit.
 * 
 * @author Ivan Jukić
 *
 */

public enum ShellStatus {

	CONTINUE, TERMINATE
}
